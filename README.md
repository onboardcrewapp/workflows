<!-- markdownlint-disable -->
# Workflows
Collections of GitHub Actions Workflow (reusable).

## Available Workflows

- [build-docker.yml](.github/workflows/build-docker.yml) – Build and push Docker images with AWS ECR, SSM helpers, and optional Slack notifications.
